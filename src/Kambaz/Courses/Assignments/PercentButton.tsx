import { Button } from "react-bootstrap";

export default function PercentageButton() {
  return (
    <Button variant="outline-secondary" className="rounded-pill px-3 fw-bold">
      40% of Total
    </Button>
  );
}